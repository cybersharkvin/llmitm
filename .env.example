# LLMitM v1.5 — Environment Configuration
#
# Target domains are configured in:
#   mitmproxy-ai-tool/.claude/settings.json → sandbox.network.allowedDomains

# Claude API Key (optional — leave blank for OAuth login)
CLAUDE_API_KEY=
