# =============================================================================
# LLMitM Bug Bounty Hunter - Environment Configuration
# =============================================================================
# Copy this file to .env and configure your target:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Target Configuration
# -----------------------------------------------------------------------------
# Your target (Juice Shop IP, bug bounty domain, etc.)
# The agent can ONLY reach Claude API + these targets.

# Domain targets (e.g., api.target.com, app.target.com)
TARGET_DOMAINS=

# IP targets (e.g., Juice Shop at 172.17.0.2, bug bounty ranges)
# Get Juice Shop IP with: docker inspect juice-shop | grep IPAddress
# TARGET_IPS=192.168.1.100,10.0.0.50,172.16.0.0/24
TARGET_IPS=172.17.0.2

# -----------------------------------------------------------------------------
# OPTIONAL: Claude API Key
# -----------------------------------------------------------------------------
# Leave blank to use interactive login (OAuth) when launching Claude.
# Set this for non-interactive/automated usage.
# Get your API key from: https://console.anthropic.com/
CLAUDE_API_KEY=

# -----------------------------------------------------------------------------
# OPTIONAL: Proxy Configuration
# -----------------------------------------------------------------------------
# Port for mitmproxy to listen on (default: 8080)
MITMPROXY_PORT=8080

# =============================================================================
# SECURITY MODEL
# =============================================================================
# This tool uses a two-container architecture:
#
#   ┌─────────────────┐      ┌──────────────────────────────┐
#   │ firewall        │◄────►│ llmitm (agent)               │
#   │ (squid proxy)   │      │                              │
#   │                 │      │ - No NET_ADMIN capability    │
#   │ ✓ Internet      │      │ - No direct internet access  │
#   │ ✓ Allowlist     │      │ - All traffic via proxy      │
#   └─────────────────┘      └──────────────────────────────┘
#
# The agent container has NO capability to modify network rules.
# Network isolation is enforced by the firewall sidecar.
#
# ADDITIONAL SECURITY: Agent cannot see docker-compose.yml or .devcontainer/
# These infrastructure files are at repo root, outside agent's /workspace mount.
# =============================================================================
